Storybook for BIM-mobile

Run storybook locally:

```bash
npm install
npm run storybook
```

Notes:
- This project uses TypeScript path alias `@/*`. The Storybook Vite config maps `@` to the repo root.
- If you see missing module types, run `npm install` to install Storybook deps.
